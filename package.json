{
  "name": "@koax/timing",
  "description": "Delay, timeout, and interval effects.",
  "repository": "git://github.com/koaxjs/timing.git",
  "version": "0.1.1",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "prepublish": "make build",
    "postpublish": "make clean",
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@koax/fork": "^0.1.3"
  },
  "devDependencies": {
    "@f/elapsed-time": "^1.0.0",
    "@koax/channels": "^0.1.2",
    "@koax/promise": "^1.0.0",
    "@koax/run": "^0.2.0",
    "babel-cli": "^6.4.0",
    "babel-polyfill": "^6.3.13",
    "babel-preset-es2015": "^6.3.13",
    "babel-register": "^6.3.13",
    "tape": "^4.2.2"
  }
}
