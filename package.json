{
  "name": "@flox/timing",
  "description": "Delay, timeout, and interval effects.",
  "repository": "git://github.com/floxjs/timing.git",
  "version": "0.2.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "prepublish": "make build",
    "postpublish": "make clean",
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/bind-middleware": "^1.1.1",
    "@flox/fork": "^0.1.3",
    "redux-flo": "^2.1.1"
  },
  "devDependencies": {
    "@f/elapsed-time": "^1.0.0",
    "babel-cli": "^6.4.0",
    "babel-polyfill": "^6.3.13",
    "babel-preset-es2015": "^6.3.13",
    "babel-register": "^6.3.13",
    "tape": "^4.2.2"
  }
}
